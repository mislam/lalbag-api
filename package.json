{
	"name": "@lalbag/api",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"dev:up": "docker compose up -d && pnpm dev",
		"dev:down": "docker compose down",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings && prettier --write worker-configuration.d.ts",
		"format": "prettier --write .",
		"db:push": "drizzle-kit push",
		"db:admin": "drizzle-kit studio"
	},
	"dependencies": {
		"@neondatabase/serverless": "^1.0.1",
		"drizzle-orm": "^0.44.4",
		"hono": "^4.8.10"
	},
	"devDependencies": {
		"dotenv": "^17.2.1",
		"drizzle-kit": "^0.31.4",
		"pg": "^8.16.3",
		"prettier": "^3.6.2",
		"wrangler": "^4.27.0"
	}
}
